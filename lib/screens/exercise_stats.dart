import 'package:flutter/material.dart';

class ExerciseStatsScreen extends StatelessWidget {
  const ExerciseStatsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Exercise Stats'),
    );
  }
}
